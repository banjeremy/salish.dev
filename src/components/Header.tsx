import { Container } from '@/components/Container';

export function Header() {
  return (
    <header className="flex flex-row justify-end items-end">
      <Container className="h-16 pt-6 w-full"></Container>
    </header>
  );
}
